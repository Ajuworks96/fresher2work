{
  "name": "fresher2work",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=api",
    "dev:recruiter": "npm run dev --workspace=recruiter-web",
    "dev:admin": "npm run dev --workspace=admin-web",
    "dev:mobile": "npm run start --workspace=mobile",
    "build": "npm run build --workspaces --if-present",
    "build:packages": "npm run build --workspace=@fresher2work/types && npm run build --workspace=@fresher2work/ui-tokens && npm run build --workspace=@fresher2work/api-client",
    "build:recruiter": "npm run build:packages && npm run build --workspace=@fresher2work/recruiter-web",
    "build:admin": "npm run build:packages && npm run build --workspace=@fresher2work/admin-web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@expo/ngrok": "^4.1.3",
    "typescript": "^5.7.3"
  }
}
